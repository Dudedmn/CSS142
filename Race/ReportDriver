import java.util.Arrays;
/**
 * Driver class that only contains a main method to run methods from the classes of
 * Race and ReportDriver.
 *
 * @author (Daniel Yan)
 * @version (December 2nd, 2017)
 */
public class ReportDriver
{
    public static void main(String[] args){
    //Creates a new RaceReport class object to use writeReport();
    RaceReport raceTime = new RaceReport();
    raceTime.writeReport();
    // Testing method
    // ReportDriver report = new ReportDriver();
    // report.testVariableValues();
    }
    
    //Method utilized only for testing in main() of ReportDriver
    // private void testVariableValues(){
    // Race racer = new Race();
    // RaceReport raceR = new RaceReport();
    // System.out.println("Array for timeInput: " + Arrays.toString(racer.timeInput()));
    // double[] unsortedArr = {1.5,99.9,3.6};
    // System.out.println("Unsorted Array: " + Arrays.toString(unsortedArr));
    // double[] testArr = racer.sortTimesAscending(unsortedArr);
    // System.out.println("Sorted Array: " + Arrays.toString(testArr));
    // //Do note that the raceTimeTies assumes that the numbers inputted are already sorted
    // System.out.print("Printing result for 0,0,0");
    // racer.raceTimeTies(0,0,0);
    // System.out.print("Printing result for 1.1,1.2,1.3");
    // racer.raceTimeTies(1.1,1.2,1.3);
    // System.out.print("Printing result for 1.1,1.1,1.3");
    // racer.raceTimeTies(1.1,1.1,1.3);
    // System.out.print("Printing result for 1.2,1.3,1.3");
    // racer.raceTimeTies(1.2,1.3,1.3);
    // System.out.print("Printing result for 1.6,1.7,1.6");
    // racer.raceTimeTies(1.6,1.7,1.7);
    // System.out.print("writeReport() for RaceReport: ");
    // raceR.testWriteReport(); //This method is originally commented out in RaceReport
    // System.out.println("choice() for Race: ");
    // racer.choice();
    
    // }
}
